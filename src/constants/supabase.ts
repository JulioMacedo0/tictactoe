export enum CHANNEL_STATES {
  closed = "closed",
  errored = "errored",
  joined = "joined",
  joining = "joining",
  leaving = "leaving",
}

export enum RESPONSE_INVITE {
  resolve = "resolve",
  reject = "reject",
  invitation = "invitation",
}
